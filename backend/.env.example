# ==============================================
# BACKEND ENVIRONMENT - Template
# ==============================================
# Copiez ce fichier vers .env et remplissez avec vos vraies valeurs
# cp .env.example .env

# Security
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-generate-with-openssl-rand-hex-32

# Database
# Pour développement local:
# DATABASE_URL=sqlite:///./app/openrift.db
# Pour production Docker:
DATABASE_URL=sqlite:///./data/openrift.db

# CORS - Update with your production domain
# Development:
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# Production:
# CORS_ORIGINS=https://openrift.cloud,http://openrift.cloud
# Both (pour tester en local contre prod):
CORS_ORIGINS=https://openrift.cloud,http://openrift.cloud,http://localhost:5173,http://localhost:3000

# Environment
# Options: development, production
ENVIRONMENT=development

# Riot Games API
# Obtenez votre clé sur: https://developer.riotgames.com/
API_RIOT=your-riot-api-key-here
RIOT_CLIENT_ID=your-riot-oauth-client-id
RIOT_CLIENT_SECRET=your-riot-oauth-client-secret
RIOT_REDIRECT_URI=http://localhost:3000/auth/riot/callback
